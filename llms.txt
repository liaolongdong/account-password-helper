# Account Password Helper（账号密码管理助手）

> Free, open-source password manager Chrome extension for developers & QA engineers. Per-field AES-256-GCM local encryption, no account registration, no subscription, and password data stored only on your machine — the single outbound request is an anonymous version check that carries no user data. One-click login (fill + tick consent + click login), multi-environment account isolation with optional cross-subdomain matching, built-in TOTP 2FA authenticator, and an independently encrypted identity vault.

## Quick Facts

- **Price**: Completely free, open-source (GPL-3.0)
- **Data storage**: browser-local only — persistent ciphertext in `chrome.storage.local`, in-memory key material in `chrome.storage.session`; the `chrome.storage.sync` store that follows your browser account is never used
- **Encryption**: PBKDF2 (600,000 iterations) + AES-256-GCM via Web Crypto API
- **Performance**: side panel returns data in about 20–50ms on the warm cache path; sub-second open across cold start, an expired session and a quick browser restart is an explicit tuning target of this project
- **Languages**: Chinese + English bilingual UI, instant switching
- **Themes**: 6 color themes (Sky Blue, Bamboo Green, Peach Pink, Blossom Mauve, Sunset Orange, Misty Slate)
- **Browser support**: Manifest V3; the side panel needs Chrome 114+ (Side Panel API) — no `minimum_chrome_version` is declared, so older Chrome simply loses side panel features
- **Account required**: No
- **Subscription**: No
- **Last updated**: 2026-09-25 · **Latest release**: v3.12.0 (2026-09-25)
- **Quality**: 1563 automated test cases across 142 test files

## Key Features

- **One-click login**: "Fill and sign in" in the side panel autofills credentials → ticks the consent checkbox → clicks the login button. `Ctrl+Shift+F` fills and ticks by default, and submits the form only after you turn on "Auto-submit login" in the preferences (off by default) — so it never clicks sign-in behind your back.
- **Multi-environment isolation**: three cross-subdomain matching tiers, set in the header of the management page and honoured by every fill entry point. "Exact match only" (the default) surfaces an entry only when its normalized hostname equals the current page; "exact + wildcard entries" additionally admits entries saved as `*.qq.com`, which then work on every subdomain of that site; "same main domain" additionally falls back to the apex (`qq.com`) and sibling subdomains (`music.qq.com`) when the current host has no entry of its own, badging each widened row as cross-subdomain. On `localhost` and `127.0.0.1` the port is always matched as well, so two local projects on `:3000` and `:5173` stay apart regardless of tier, and the auto-save duplicate rule is deliberately tier-independent — widening never changes which entry a save overwrites
- **Built-in TOTP 2FA authenticator**: RFC 6238 codes generated locally, GitHub-style two-step login auto-anchors a live code capsule — no phone authenticator app needed
- **Per-field AES-256-GCM encryption**: PBKDF2 (600,000 iterations) + AES-256-GCM via Web Crypto API; username, password, URL, remark and TOTP secret are encrypted field by field before touching disk, and password data is never uploaded
- **Identity vault**: a separate, independently encrypted store for personal identity data (ID numbers, bank cards, passports, custom fields) with two-level format validation (checksum errors block, everything else warns), whole-card copy, save-time duplicate detection, encrypted `.aphid` backup and optional re-importable plaintext `.json` export — reachable only from the Options page and never surfaced in the side panel
- **Offline security audit**: one-click 0–100 score weighted across four dimensions (reused 35 / weak 25 / commonly leaked 20 / stale 20), with missing-2FA reported separately but unscored — all computed on-device
- **Password generator**: random mode (configurable length/charset/ambiguous-character exclusion) + passphrase mode drawing from a bundled 3,080-word list, Web Crypto CSPRNG
- **Quadruple fill strategy**: inline fill (key icon in input, sharing the side panel's ordering and multi-field pinyin search), side panel one-click fill, right-click context menu fill, shortcut quick-fill
- **Auto-save credentials**: Chrome-style login capture with smart dedup, domain allow/block lists; the save prompt flags weak and reused passwords inline (a heads-up only — it never blocks saving)
- **Import / export**: CSV & JSON; auto-detects Chrome, LastPass, Bitwarden, 1Password exports
- **Site rules for stubborn login pages**: pin custom CSS selectors for the username and password fields of a given domain, and optionally reach fields nested inside shadow DOM — for self-built login pages and component-library forms that heuristic detection gets wrong. Rules export to a plain-text JSON file (domains and selectors only, never credentials) and merge-import by domain with an added / updated / skipped report, capped at 500 rules and 2 MiB per file
- **Command palette**: `Ctrl/Cmd + K` on the management page filters its 23 commands — add entry, health check, import / export, encrypted backup, trash, site rules, cross-subdomain matching, change master password, preferences — by Chinese, pinyin or initials; it opens only once you are unlocked, and destructive commands keep their confirmations
- **Share card**: one click on a side panel row or in the read-only details drawer composes "username / password / URL" into a single plain-text clipboard payload, subject to the same timed clipboard wipe as every other copy
- **6 color themes + bilingual UI** (中文 / English), instant switching without refresh
- **Password visibility toggle**: injects show/hide button into page password fields, verify filled content with one click
- **Trash bin & password history**: 30-day soft delete, encrypted snapshots per entry (default 3, configurable 1-10), one-click rollback
- **Smart search**: pinyin/initials search with match highlighting, side panel "This site / All entries" scope toggle (off-site hits stay copyable and open in a new tab), tags, favorites with LRU eviction
- **Side panel quick add**: the "+" in the panel header saves an account in place with the current domain prefilled; the empty state offers the same shortcut when the site has no saved account
- **Read-only entry details**: a per-row drawer shows the full remark, 2FA code, password history and timestamps without entering edit mode; copied passwords auto-clear per the clipboard setting
- **Caps Lock warning**: live uppercase-lock detection on every master password field, so a typo is never mistaken for a wrong password
- **Flexible session control**: 1 hour to 7 days validity, auto idle lock, lock on browser restart

## Who It's For

- **Developers**: manage dev / staging / prod accounts for the same site with exact-domain isolation by default, and switch to wildcard or same-main-domain tiers when one account legitimately serves a whole subdomain family
- **QA engineers**: quickly switch test accounts, one-click login across environments
- **Privacy-conscious users**: per-field local encryption, passwords never uploaded, no account registration
- **Everyday users**: stop memorizing passwords, built-in TOTP two-factor authentication
- **Migrating users**: one-click import from Chrome, LastPass, Bitwarden, 1Password — CSV & JSON, done in 30 seconds

## FAQ

**Will my passwords be uploaded to the cloud?**
No. All data stays in your browser's local storage. Sensitive fields (username, password, URL, remark, TOTP secret) are encrypted with AES-256-GCM before they touch disk, and vault content never travels over the network. There is no server, no account and no sync. The only outbound requests are anonymous version checks — a reachability probe of chromewebstore.google.com (cached 24h) and, when the store is unreachable, a read-only call to api.github.com — which carry no vault content and no personal identifier.

**What happens if I forget the master password?**
It cannot be recovered — the encryption key is derived from it (PBKDF2, 600,000 iterations). Use "Reset" to wipe the vault and start over. Back up regularly via encrypted .aph export to avoid data loss.

**What happens when the session expires?**
Nothing is lost and nothing needs re-encrypting — the vault is ciphertext at rest. Expiry or locking destroys the session key material and the decrypted cache, marks the session locked, and clears the in-memory mirrors. Re-enter the master password to derive the key again and every entry is back.

**Can I import from other password managers?**
Yes. CSV & JSON import auto-detects Chrome, LastPass, Bitwarden and 1Password export formats, with Chinese/English column-name mapping. Typical migration takes about 30 seconds.

**Can one account serve a whole subdomain family?**
Yes, on request. The default tier matches hosts exactly; "exact + wildcard entries" admits entries saved as `*.qq.com` on every subdomain of that site, and "same main domain" adds the apex and sibling subdomains as a fallback, badged as cross-subdomain. Widening is display-and-fill only: the auto-save duplicate rule is deliberately tier-independent, so a wider tier never changes which entry a save overwrites, and `localhost` / `127.0.0.1` stay separated by port.

**What if the login page confirms the field detection?**
Add a site rule for that domain: your own CSS selectors for the username and password fields, plus an optional shadow DOM penetration switch for forms rendered inside custom elements. Rules travel as plain-text JSON (domains and selectors, never credentials) and merge-import by domain with an added / updated / skipped report, so a team can share one working set instead of configuring browser by browser.

## How It Compares

| Feature | Account Password Helper | Bitwarden | 1Password | Chrome Built-in |
|---------|:---:|:---:|:---:|:---:|
| Price | Completely free | Free / Paid | $2.99/mo | Free |
| Data storage | Pure local | Cloud | Cloud | Local |
| Account required | No | Yes | Yes | No |
| One-click login (fill + tick + click) | Yes | Fill only | Fill only | Fill only |
| Multi-environment isolation | Yes (exact host by default) | No | No | No |
| Site rules: per-domain custom CSS selectors, shadow DOM reach | Yes | No | No | No |
| Command palette with pinyin search (`Ctrl/Cmd + K`) | Yes | No | No | No |
| Built-in TOTP authenticator | Yes | Paid tier | Paid tier | No |
| Offline security audit | Yes | No | No | No |
| Open source (GPL-3.0) | Yes | Yes | No | No |

> Competitor cells describe behaviour documented publicly as observed in 2026-09; vendors change features frequently, so check their sites before relying on a row.

## Security Architecture

- Master password → PBKDF2 (600,000 iterations) → 256-bit key
- Plaintext + key + random IV → AES-256-GCM → Base64(IV + ciphertext)
- Sensitive fields encrypted: username, password, URL, remark, TOTP
- Session data key: PBKDF2-SHA256 (600,000 iterations) derived from the master password; while the session is valid it is cached in Service Worker memory and `chrome.storage.session` (memory only) and kept in `chrome.storage.local` wrapped by a random wrap key, so the master password itself is never written to disk
- Session expiry / lock: the session key material and decrypted cache are destroyed; ciphertext at rest is left untouched
- Auto-lock triggers: idle timeout, system lock, browser restart (optional)
- Clipboard auto-wipe after copying passwords (default 30s, configurable)
- HKDF appears only on the legacy `session_master_password` migration path, never in the current session flow
- Everything is computed locally; the only network activity is the anonymous version check described in the FAQ

## Technical Stack

- **Framework**: WXT + Vue 3 + TypeScript
- **UI**: Element Plus (on-demand import)
- **Build**: Vite
- **Crypto**: Web Crypto API (native browser implementation)
- **Manifest**: V3 (latest Chrome extension standard)
- **Testing**: Vitest

## Installation

- **Chrome Web Store**: https://chromewebstore.google.com/detail/account-password-helper/fgimkdodpjfkddmildjieojpfakpanli
- **GitHub Releases** (offline install): https://github.com/liaolongdong/account-password-helper/releases/latest
- **From source**: `pnpm install && pnpm build` → load `.output/chrome-mv3/` as unpacked extension

## Links

- [Chrome Web Store](https://chromewebstore.google.com/detail/account-password-helper/fgimkdodpjfkddmildjieojpfakpanli)
- [GitHub Repository](https://github.com/liaolongdong/account-password-helper)
- [Live Demo & Docs](https://liaolongdong.github.io/account-password-helper/)
- [English Version](https://liaolongdong.github.io/account-password-helper/en.html)
- [Tech Blog (中文)](https://liaolongdong.github.io/account-password-helper/blog/) — 5 篇技术文：本地优先产品故事、MV3 侧边栏秒开、Web Crypto 加密实战、四个新功能的实现笔记、跨子域名匹配三档的判据与踩坑
- [Tech Blog (English)](https://liaolongdong.github.io/account-password-helper/blog/index.en.html) — the same five posts in English, including how the three cross-subdomain tiers decide which accounts appear
- [Pricing](https://liaolongdong.github.io/account-password-helper/pricing.html) — 100% free, no paid tier, no subscription
- [Pricing (machine-readable)](https://liaolongdong.github.io/account-password-helper/pricing.md)
- [Alternatives Comparison (中文)](https://liaolongdong.github.io/account-password-helper/compare.html) — Bitwarden / 1Password / Chrome 自带替代方案十维度事实对比
- [Alternatives Comparison (English)](https://liaolongdong.github.io/account-password-helper/compare.en.html) — Bitwarden alternative, 1Password alternative and Chrome built-in compared across 10 dimensions
- [Architecture & Implementation Details](https://github.com/liaolongdong/account-password-helper/blob/main/docs/ARCHITECTURE.md)
- [Contributing Guide](https://github.com/liaolongdong/account-password-helper/blob/main/docs/CONTRIBUTING.md)
- [Changelog](https://github.com/liaolongdong/account-password-helper/blob/main/CHANGELOG.md)

## More by this author

The same author builds open-source, offline-first Chrome extensions where data stays on your machine.

- [Transfer Any File](https://github.com/liaolongdong/transfer-any-file) — 14 file formats converted to each other entirely in the browser, without uploading a byte: Markdown, Word, PDF, Excel, CSV, JSON, HTML and images, with mixed-format batches, automatic multi-step chains, preview and inline editing, and ZIP packaging. No account, no upload, no network request. [Product page](https://liaolongdong.github.io/transfer-any-file/)
- [Cross-origin Proxy](https://github.com/liaolongdong/cross-origin-proxy) — proxies a page's API requests to another backend environment: rewrite URLs, headers and responses, mock conditionally, inject delays, block requests, retry failures and forward WebSocket, all configured in the browser. This extension covers "who am I in this environment", that one covers "where this environment's requests go". [Product page](https://liaolongdong.github.io/cross-origin-proxy/en.html) · [Chrome Web Store](https://chromewebstore.google.com/detail/dednngakllblfilbndkaggphohmpgcbg)
- [Author page](https://github.com/liaolongdong)

## License

GPL-3.0-only. Free to use, modify and distribute (including commercially). Derivative works must be open-sourced under GPL-3.0.
