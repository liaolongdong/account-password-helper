# 落地页与扩展动效提案（Motion Proposal）

**日期**：2026-09-24 · **分支**：feature-opt · **状态**：待拍板，未落地
**配套实物**：[`docs/prototypes/landing-motion-lab.html`](./prototypes/landing-motion-lab.html) —— 14 条候选已做成可重播、可模拟「减弱动效」、可模拟「禁用 JS」的预览页，编号与本文一一对应。

---

## 0. 结论先行

三件事按顺序做，其余都是配菜：

1. **先补地基，再加动效**：本站 `index.html` 的动效层缺三样兄弟站已有的东西——`html.js` 门控、时长令牌、循环动效的视口门控（`is-live`）。其中 `html.js` 门控是**缺陷**：21 个 `.reveal` 节点（含 `#compare` 整节）在没有 JS 时永久 `opacity: 0`。地基不补，后面每加一条动效就多吞一块内容。
2. **只做「让功能自己讲自己」的动效**，不做氛围装饰。最值得做的 4 条：D1 填充流水线跑包（f2）、D2 TOTP 活码倒计时环（f4）、D4 跨子域三档匹配扫描（f14）、D3 安全体检评分计数（f5）。它们的共同点是：动效本身就是产品差异化的说明图，关掉动效这屏就讲不清了。
3. **动效对 SEO 的作用只有一条是硬的**：别把 LCP/CLS 弄坏。想涨曝光，钱要花在元数据、社交预览图、内链与商店文案上（见 §6，本文只交代动效能沾边的部分，不夸大成排名因素）。

**需要点头的只有一处**：B 档（D9 卡片追光、D12 轮播修正、F1 门控）要在落地页新增约 40~60 行内联 JS，这会打破你 2026-09-18 定的「克制专业档 + 纯 CSS、不新增 JS」。A 档完全不碰 JS。

---

## 1. 现状事实（全部实读，非推测）

### 1.1 本站落地页已有

| 位置                            | 内容                                                                                                                                                                                                                          |
| ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `index.html:1522-1705`          | Motion layer：`aph-fade` / `aph-rise` / `aph-float` / `aph-bob` / `aph-sheen` / `aph-shackle` 六组关键帧；hero 分层入场、钥匙卡浮动 + 掠光、锁扣闭合、步骤数字与 FAQ 淡入、三处悬停微交互（特性图标 / 导航下划线 / 互链箭头） |
| `index.html:1802-1847`          | 唯一一处全局降级块，原则是「去位移、留淡入」                                                                                                                                                                                  |
| `index.html:439`                | 只有 `--ease-out` 一个缓动令牌，**没有时长令牌**，各处写死 `0.6s` / `0.25s` 字面量                                                                                                                                            |
| `index.html:1495-1520`          | `.reveal{opacity:0}` + `.reveal-delay-1..5` 手工错峰                                                                                                                                                                          |
| `index.html:4195-4209`          | 轮播进度条用 `width` 过渡 + 每轮 `void offsetWidth` 强制 reflow；自动播放不随减弱动效停、不随离屏停                                                                                                                           |
| `compare.html` / `privacy.html` | keyframes 0 处、reduced-motion 0 处（`pricing.html` 各 2 / 1）；`en.html` 是 `pnpm gen:en` 产物，与 `index.html` 同步                                                                                                         |

### 1.2 兄弟站已有、本站缺失的动效语汇

从 `transfer-any-file`（11 组 keyframes，内联）与 `cross-origin-proxy`（11 组 keyframes + `assets/landing.css` / `landing.js`）提取：

| 机制           | 兄弟站写法                                                       | 本站                        |
| -------------- | ---------------------------------------------------------------- | --------------------------- |
| JS 门控        | `html.js .reveal {…}`，脚本第一行 `root.classList.add('js')`     | ✗ 缺（→ F1）                |
| 时长/缓动令牌  | `--dur-fast/mid/slow` + `cubic-bezier(0.16,1,0.3,1)`             | 仅 `--ease-out`             |
| 错峰           | JS 写 `--lp-rank`，样式 `calc(var(--lp-rank) * 70ms)`            | 手工 `.reveal-delay-N`      |
| 循环省电梯的电 | `.flow:not(.is-live)` 先 `animation-play-state: paused`，IO 放行 | ✗ 缺（hero 掠光是常驻循环） |
| 数据包跑线     | `lp-travel` / `lp-hop` / `wire`                                  | ✗ 无                        |
| 复制回执       | `confirm-pop`（变绿 + 弹一下）                                   | ✗ 无                        |
| 锚点落位       | `section:target h2 { animation: found 1.2s }`                    | ✗ 无                        |
| lightbox 缩放  | `plate-in`（从缩略图尺寸放大，制造同一张图的连续感）             | ✗ 无灯箱                    |
| 卡片追光       | `--lp-x/--lp-y` + `rAF` 攒帧 + `z-index:-1`/`isolation`          | ✗ 无                        |
| 轮播进度       | `gallery-autoable` + IO + 减弱动效不自动播                       | 用 `width`，未降级          |

### 1.3 扩展内部（C 档的依据）

| 面           | 现状                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TOTP 活码    | `components/TotpCode.vue:161` 已有 `stroke-dashoffset .95s linear` 环 + `:76` `is-expiring`（≤5s）+ `:221` 减弱动效块。**已经是一条可直接搬到货架上的真动效**                                                                                                                                                                                                                          |
| 安全体检     | `components/options/PasswordHealthDialog.vue:430/642-661` 已有 `animatedScore` 从 0 递增 + `:757` 环过渡                                                                                                                                                                                                                                                                               |
| 侧边栏首屏   | `entrypoints/sidepanel/index.html:37-62` 骨架 `sk-pulse` + `.fade-out`，且已有减弱动效块；`App.vue:936-946/1009` 的 `finishOpen` 受 `nextTick → rAF` 门控                                                                                                                                                                                                                              |
| 内容脚本 UI  | `entrypoints/content/floatingButtons/styles.ts:29` 用 `transition: left .3s …`（**动的是布局属性**）、`:55/:74` `transition: all`；折叠态 `:132` 用 `transition: none !important` 硬切。Closed Shadow DOM，keyframes 随影子树（`SavePasswordPrompt.ts:33`）                                                                                                                            |
| 选项页表格   | `components/options/PasswordTable.vue:551-581` `fade-in`（实为 `translateY(-20px)`）与 `fade-out`（`translateX(800px)`，1s）——删除一行会横向飞 800px                                                                                                                                                                                                                                   |
| 减弱动效覆盖 | 有：`TotpCode` / `PasswordHealthDialog` / `HeaderBar` / `CommandPalette` / `IdentityVaultDialog` / `SidepanelAuthView` / `sidepanel/index.html` / `FillFailurePrompt`。**无**：`styles.ts`、`SavePasswordPrompt`、`InlineFillDropdown`、`TotpHandoffCapsule`、`PasswordTable`、shake（`MasterPasswordVerifyDialog` / `PasswordVerifyView`）、`PasswordListItem`、`Popup`、`HelpDialog` |
| 动效令牌     | `assets/theme/tokens.css` 69 个 `--aph-*`，**没有任何时长/缓动变量**                                                                                                                                                                                                                                                                                                                   |

---

## 2. 功能 → 动效映射（14 项特性逐个过筛）

打分口径：**差异化**（竞品有没有）× **可视化难度**（能不能一眼看懂）× **调性一致**（安全工具要克制，不要玩具感）。

| 特性                  | 差异化     | 可视化                | 调性                   | 判定                      |
| --------------------- | ---------- | --------------------- | ---------------------- | ------------------------- |
| f2 四种方式一键填充   | 高         | 强（流水线天然动）    | ✓                      | **做**（D1）              |
| f4 TOTP 活码          | 高         | 强（倒计时就是动效）  | ✓                      | **做**（D2）              |
| f14 跨子域匹配三档    | 极高（新） | 强（扫描 + 命中）     | ✓                      | **做**（D4）              |
| f5 安全体检 0~100     | 中高       | 强（计数 + 权重条）   | ✓                      | **做**（D3）              |
| f1 纯本地加密         | 极高       | 中（字段变密文可画）  | ✓                      | **做**（D5，克制版）      |
| f3 智能识别表单       | 中         | 中（框线游走扫描）    | △ 易显玩具感           | 缓（可做 D1 的变体）      |
| f6 自动保存凭证       | 中         | 中（卡片浮入 → 落袋） | △                      | 缓                        |
| f7 导入导出 .aph      | 中         | 低                    | ✓                      | 不做（静态图示更清楚）    |
| f8 密码生成器         | 中         | 强（字符翻滚）        | ✗ 与「保险柜」调性冲突 | 不做                      |
| f9 回收站与历史       | 中         | 低                    | ✓                      | 不做（用 D9 追光承载）    |
| f10 换肤与双语        | 中         | 中（主题交叉淡入）    | ✓                      | 可选（D14，低优先）       |
| f11 标签/拼音智能搜索 | 中         | 强（输入 → 命中高亮） | ✓                      | 可选（D1 变体，命中点亮） |
| f12 身份信息库        | 高         | 中                    | ✓                      | 不做动效，做卡片内容      |
| f13 站点规则兜底      | 中         | 低                    | ✓                      | 不做                      |

明确不做的：粒子/雪花背景、鼠标拖尾、全屏开场动画、自动播放配音演示、任何遮挡文字的循环。密码管理器的第一屏情绪必须是**可信赖**，不是热闹。

---

## 3. A 档：纯 CSS，零新增 JS（建议先做这批）

落点全部在 `index.html` 的 `</style>` 前 Motion layer 区块；降级条目一律追加到末尾降级块。

| #   | 候选                            | 落点选择器                                                                                           | 实现要点                                                              | 降级                                             | 规模                                   |
| --- | ------------------------------- | ---------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ------------------------------------------------ | -------------------------------------- |
| A1  | D1 填充流水线跑包               | f2 卡内新增 `.flow`（4 节点 + 3 连线，纯 `<span>`）                                                  | 1 组 `travel` + 1 组 `wire`，节点 `animation-delay: 0/.5/1/1.5s`      | 停在静态示意帧，包隐藏                           | CSS ~60 行                             |
| A2  | D2 活码环 + 换码淡入            | f4 卡内静态 SVG                                                                                      | `stroke-dashoffset` 30s 线性 + ≤5s 变红呼吸（与 `TotpCode.vue` 同款） | 环静止、数字不动，保留淡入                       | CSS ~40 行                             |
| A3  | D4 三档匹配扫描                 | f14 卡内 `.tiers`                                                                                    | `::before` 渐变扫描带 `translateX`，命中行蓝框 + 「命中 · 可见」常驻  | 去扫描带，命中态仍在                             | CSS ~45 行                             |
| A4  | D3 体检计数 + 权重条            | f5 卡内                                                                                              | `@property --score` + `counter()`；四条 `scaleX`（**不动 `width`**）  | 直接呈现终值 82 与满态                           | CSS ~50 行                             |
| A5  | D5 字段密文掠光                 | f1 卡或 hero `.key-card`，复用现有 `aph-sheen`                                                       | 明文点 → 密文用两帧 `<span>` 交叉淡入                                 | 只显示密文态                                     | CSS ~30 行                             |
| A6  | D6 对比表 own 列扫光 + 对勾错峰 | `#compare .compare-table td:nth-child(2)`（**本插件列无专属 class，用 nth-child 免改 30 个单元格**） | 纵向 `translateY` 扫光 + `lab-pop` 错峰 80ms                          | 扫光 `opacity:0`，对勾直接在场                   | CSS ~35 行                             |
| A7  | D8 `:target` 落位回色           | `section:target .section-head h2, #faq .faq-item:target`                                             | 一条 `found` 关键帧，纯 CSS                                           | `animation: none`，色彩反馈留在 `:target` 静态态 | CSS ~8 行                              |
| A8  | D11 步骤连线绘制                | `#steps .step-grid` 叠一层 SVG `stroke-dashoffset`                                                   | 线先绘、序号再错峰点亮（复用现有 `.step-num` 钩子）                   | 直接呈现完整线与序号                             | CSS ~30 行，需窄屏 `display:none` 分支 |
| A9  | D13 首屏入场改法                | `index.html:1581-1609`                                                                               | LCP 那一行改「只位移不淡入」，延迟收进 ≤80ms；错峰改用 `--rank`       | 降级块里连 `animation-delay` 一起归零            | 改既有 ~20 行                          |

**A 档合计约 300 行 CSS，零 JS 风险。** 建议 A1→A4→A3→A6→A7 首批，A5/A8/A9 次批。

---

## 4. B 档：需要一小段内联 JS（要你点头）

| #   | 候选                                       | 落点                                                                             | 脚本量                                           | 为什么值得                                                                                               |
| --- | ------------------------------------------ | -------------------------------------------------------------------------------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------- |
| B1  | F1 `html.js` 门控（**修缺陷**）            | `index.html` 脚本首行 + 动效选择器加前缀；同时给 `revealObserver` 补 IO 缺席兜底 | 1 行 + 选择器前缀                                | 21 个节点、含 `#compare` 整节，禁用 JS 时现在是全隐形的；`<noscript>` 只兜了 FAQ                         |
| B2  | F2 轮播进度条改 `scaleX` + 离屏/减弱动效停 | `index.html:4195-4209` `start()/stop()`                                          | ~15 行改写                                       | 动效层自己的硬约束是「只动 transform/opacity」，现在是 `width` + 每轮强制 reflow；减弱动效下自动轮播该停 |
| B3  | D9 卡片追光                                | `.feature-card` / `.more-card`                                                   | ~18 行（`pointermove` → `rAF` 攒帧写 `--x/--y`） | 兄弟站已有，14 张卡的质感差一截；`hover:hover + pointer:fine` 与减弱动效下压根不绑定                     |
| B4  | 循环动效视口门控（is-live）                | hero 掠光 + A1/A3 的新循环                                                       | ~10 行                                           | 常驻循环在白板上是白耗电；这条和 F2 同源，一起做最省                                                     |
| B5  | D12 侧边栏秒开演示带                       | 新增「性能承诺」小带                                                             | 0（纯 CSS 秒表）                                 | 数字必须先由 `benchmarks/` 跑出可复现口径再写，**不许凭感觉写 <1s**                                      |

F1/F2 我认为属于「该修」而不是「加料」；B3 纯属审美提升，可以单独否掉。

---

## 5. C 档：扩展内 UI（另立波次，不与落地页混做）

前置：`assets/theme/tokens.css` 补 `--aph-duration-fast/mid/slow` 与 `--aph-ease-out`，把散落各处的 `.2s/.25s/.3s` 与两种 `cubic-bezier` 收敛成令牌（纯内部、可证行为等价）。

| #   | 候选                 | 现状 → 目标                                                                                                            | 红线                                                 |
| --- | -------------------- | ---------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| C1  | 悬浮按钮折叠动画     | `styles.ts:29` 动 `left`（布局属性）→ 改 `transform: translateX()`；`:132` 的 `transition: none !important` 硬切保留   | 不得改变可点区域与出现时机                           |
| C2  | 内容脚本 UI 补降级   | `styles.ts` / `SavePasswordPrompt` / `InlineFillDropdown` / `TotpHandoffCapsule` 无减弱动效块 → 统一「去位移、留淡入」 | 影子树内写 keyframes，不外泄                         |
| C3  | 选项页删除行飞 800px | `PasswordTable.vue:572` `translateX(800px)` 1s → 160ms 淡出 + 轻微 `translateX(-8px)`                                  | 行高变化仍走现有布局，避免表格抖动                   |
| C4  | shake 补降级         | `MasterPasswordVerifyDialog` / `PasswordVerifyView` 的 400ms 抖动无减弱动效块                                          | 减弱动效下换成边框变红 + 文案，不能只剩静默          |
| C5  | 侧边栏列表入场       | `PasswordListItem` 只有 hover 过渡，列表出现是硬切 → 首屏 ≤30 行错峰淡入（`INITIAL_RENDER_COUNT=30` 已存在）           | **绝不能加进 `finishOpen` 的 rAF 链**，秒开 SLA 优先 |
| C6  | 换肤交叉过渡         | 6 套主题切换现在是瞬时重绘 → `background-color .2s`（仅根层）                                                          | 不得让 Shadow DOM 内联令牌与页面脱拍                 |

C5/C6 直接触及「侧边栏秒开」与主题一致性，按仓库规则属于**必须先给方案再动手**的一类，本文只登记不实施。

---

## 6. SEO / 曝光：动效的真实边界

诚实拆开，避免把动效当排名因素卖给你：

- **动效不改变排名**。Google 的排名因素里没有「页面有动画」。
- **动效能碰的三条**：① 核心网页指标不劣化（甚至因 A9 而略好）；② 行为信号（有效停留、滚动深度、`#compare` 是否看完、FAQ 展开率）——这些是观测指标，不是已证实的排名因素，别当 KPI 承诺；③ 外部落地体验：D8 `:target` 回色让搜索/富结果/Blog 跳进来的人一眼确认落点，是这批里唯一「纯 CSS、零成本、直接服务流量入口」的条目。
- **`html.js` 门控（B1）才是这批里唯一有 SEO 硬理由的**：无 JS 抓取路径（部分 AI 爬虫、`Google-Extended` 类）现在读不到对比表和 14 张特性卡的可见文本。
- **同批值得顺手做但不属于动效的**（若你点头可并入）：`llms.txt` 的用例数/文件数与 `sitemap.xml` 的 `lastmod` 是易漂移字段，2026-09-22 实测基准 1337 用例 / 119 文件，落地页当前写作多少需核对；`compare.html`（承接「1Password 替代」「LastPass 替代」这类高意图词）现在零动效零降级，可与 A6 共用资产。

---

## 7. 落地顺序与验证矩阵

1. **地基**（B1 → B2 → 时长令牌 F3）：先让动效层有 `html.js` 前缀、`--dur-*`、`is-live` 三件东西。
2. **A 档首批**（A1 A4 A3 A6 A7）：每 2 条一次提交，方便回滚定位。
3. **A 档次批 + B3**（视你对 JS 的点头）。
4. **同步产物**：`pnpm gen:en`（`en.html` 是生成物，勿手改）；A 档新增的用户可见装饰文案（如「命中 · 可见」）必须进 `index.html` 的 I18N 字典 `zh` + `en` 两处，纯装饰 `<span>` 用 `aria-hidden="true"`，不用 `content:'→'`（会被朗读且中英不一致）。
5. **验证**（全绿才算完成）：

| 验证         | 命令 / 方法                                                                                                                                                                                                                                                                              | 通过标准                                                                                                                                                                                                             |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 格式         | `pnpm exec prettier --check docs/prototypes/landing-motion-lab.html`（已 ✓）；`index.html` 只跑 `--check` 读结果，**不跑 `--write`**                                                                                                                                                     | HEAD 上 `index.html` 本来就有 2 处遗留差异，别为此多出无关 hunk。另：prettier 会统一 `<style>` 内的相对缩进（lab 落盘后是 4 空格基准，`index.html` 是 6 空格基准），落地时沿用目标文件既有缩进，别把两种基准混在一处 |
| 类型/静态    | `pnpm typecheck`、`pnpm lint`                                                                                                                                                                                                                                                            | 0 error（A 档只碰 HTML/CSS，本步是防手滑）                                                                                                                                                                           |
| 构建         | `pnpm build`                                                                                                                                                                                                                                                                             | 落地页不进包，但要确认没碰 `public/`                                                                                                                                                                                 |
| 真浏览器三态 | 已对 lab 页做过：真实 Chrome（headless）枚举 14 卡计算样式，`getAnimations().length = 60`；`html.rm` → `flow/scan/sheen/sweep/shine = none`、错峰转纯淡入；`html` 去 `js` → 装饰动画消失、`min-height` 占位仍在、`.good .reveal` opacity 1 而 `.bad .reveal` opacity 0（隐形缺陷可见证） |
| 减弱动效实测 | 裸 CDP（`Emulation.setEmulatedMedia` 走真 `-webkit-media` 生效路径，MCP 的 `emulate` 没有这档时按既有笔记用 Node WebSocket）                                                                                                                                                             | 无位移、无循环、淡入保留                                                                                                                                                                                             |
| 性能         | `chrome-devtools` 的 `performance_start_trace` / `lighthouse_audit`，`index.html` 改动前后各一次，同机同口径                                                                                                                                                                             | LCP / CLS / TBT 三项不劣化；A9 若声称有收益，给前后毫秒数                                                                                                                                                            |
| 无 JS        | `--disable-javascript` 起 Chrome 截全页                                                                                                                                                                                                                                                  | 首屏文案、14 张特性卡、对比表、FAQ 全部可见                                                                                                                                                                          |
| 视觉回归     | 真 Chrome 渲染 `file://` 页面（`--load-extension` 在 stable 已废，MCP 浏览器带 `--disable-extensions`，按既有笔记克隆 profile + `--app`）                                                                                                                                                | 6 套配色 + 中英两版无破版                                                                                                                                                                                            |

---

## 8. 风险与未验证项

1. **A9 的 LCP 收益是机制推断，不是实测结论**：`both` + `animation-delay` 会把元素按在 `opacity:0` 的起始帧，全透明期间不作为 LCP 候选；但本站首屏 LCP 元素究竟是 `h1` 文字还是 `.key-card` 渐变块、差多少毫秒，**未测**。没有前后数据就不写进任何对外文案。
2. **A4 依赖 `@property`**（Chrome 85+/Safari 16.4+/Firefox 128+）。不支持时数字必须仍显示终值而不是 0，落地时要把「基准值 = 终值、动画只负责走过去」写清，否则老浏览器出现「82 变 0」的倒退。
3. **A6 用 `td:nth-child(2)` 绑本插件列**：将来表头增删列会静默错位。若接受，就补一条注释；不接受则要给该列加 class（改 30 个单元格，diff 变大）。
4. **D5 的假密文与假凭据**：必须一眼看出是装饰（长度截断 + 等宽 + 灰化），且不能长得像真实凭据格式；同族教训见仓库里旧截图 PII 那次。
5. **B2 会改变轮播既有行为**（减弱动效下不再自动播）。按仓库规则属「影响交互」，需明确确认后再动。
6. **C 档不在本波**：C5/C6 触及秒开 SLA 与换肤一致性，属先方案后实施。
7. lab 页已用 `noindex, nofollow`，且 `docs/prototypes/` 不在 Pages 发布路径内；若要挂到官网需另议。

---

## 9. 需要你回答的三个问题

1. **JS 授权**：只做 A 档（零 JS）？还是 A + B1/B2（修缺陷级小 JS）？还是 A + B 全部（含卡片追光）？
2. **范围**：只 `index.html`，还是同时把 A6/D8 铺到 `compare.html`（零动效的高意图承接页）？
3. **C 档**：本波完全不碰扩展 UI，还是另立一波只做 C1/C2/C4（都是补齐既有约束，不改交互）？
