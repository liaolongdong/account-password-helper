# 🔐 主动清除主密码会话功能

## 📋 功能概述

在密码列表的有效期设置弹窗中新增了"清除当前会话"功能，允许用户主动清除当前的主密码会话，提高安全性和用户体验。

## ✨ 功能特性

### 1. 入口位置

- **位置**: 密码列表 → 有效期设置弹窗
- **显示条件**: 仅当存在有效会话时显示
- **按钮样式**: 红色危险按钮，带删除图标

### 2. 交互流程

1. 用户点击"清除当前会话"按钮
2. 弹出确认对话框，说明清除后的影响
3. 用户确认后执行清除操作
4. 显示操作结果提示

### 3. 清除操作内容

- 清除内存中的会话主密码
- 清除会话过期时间
- 清除持久化存储的会话信息
- 重置认证状态
- 清空密码列表显示
- 停止会话定时器

## 🔧 技术实现

### 1. UI 组件修改

```vue
<!-- 在有效期设置弹窗的会话状态区域添加清除按钮 -->
<div class="session-actions" style="margin-top: 8px;">
  <el-button
    type="danger"
    size="small"
    :icon="Delete"
    @click="handleClearSession"
    :loading="clearSessionLoading"
  >
    清除当前会话
  </el-button>
</div>
```

### 2. 状态管理

```typescript
// 添加清除会话加载状态
const clearSessionLoading = ref(false);
```

### 3. 核心处理函数

```typescript
const handleClearSession = async () => {
  try {
    // 确认对话框
    await ElMessageBox.confirm(
      '确定要清除当前主密码会话吗？清除后需要重新输入主密码才能访问密码列表。',
      '确认清除会话',
      {
        confirmButtonText: '确定清除',
        cancelButtonText: '取消',
        type: 'warning',
        confirmButtonClass: 'el-button--danger'
      }
    );

    clearSessionLoading.value = true;

    // 清除会话
    await StorageUtils.clearSession();

    // 更新UI状态
    await updateSessionInfo();
    stopSessionTimer();
    isAuthenticated.value = false;
    passwords.value = [];

    ElMessage.success('主密码会话已清除');
  } catch (error) {
    if (error !== 'cancel') {
      ElMessage.error('清除主密码会话失败');
    }
  } finally {
    clearSessionLoading.value = false;
  }
};
```

## 🎯 使用场景

### 1. 安全场景

- 用户离开电脑时主动清除会话
- 在公共电脑上使用后清除会话
- 怀疑会话泄露时立即清除

### 2. 功能场景

- 切换用户账户时清除当前会话
- 重新设置主密码前清除旧会话
- 测试功能时重置会话状态

## 🔒 安全考虑

### 1. 确认机制

- 必须通过确认对话框才能执行清除操作
- 明确说明清除后的影响和后果

### 2. 状态重置

- 完全清除所有会话相关数据
- 重置UI状态到未认证状态
- 停止所有相关定时器

### 3. 错误处理

- 捕获并处理清除过程中的错误
- 提供用户友好的错误提示

## 📱 用户体验

### 1. 视觉设计

- 使用红色危险按钮突出操作的重要性
- 添加删除图标增强识别度
- 加载状态提供操作反馈

### 2. 交互设计

- 确认对话框防止误操作
- 清晰的操作结果提示
- 平滑的状态过渡

### 3. 信息提示

- 明确说明清除操作的影响
- 提供操作成功/失败的反馈
- 保持界面状态的一致性

## 🧪 测试验证

### 1. 功能测试

- [x] 按钮在有效会话时正确显示
- [x] 确认对话框正常弹出
- [x] 清除操作成功执行
- [x] 状态正确重置
- [x] 错误处理正常

### 2. 构建测试

- [x] 项目构建成功
- [x] 无语法错误
- [x] 类型检查通过

## 📝 更新日志

### v1.0.0 - 2024-12-19

- ✅ 新增主动清除主密码会话功能
- ✅ 在有效期设置弹窗中添加清除按钮
- ✅ 实现完整的清除逻辑和状态管理
- ✅ 添加确认对话框和错误处理
- ✅ 优化用户体验和界面设计

## 🔄 后续优化建议

1. **快捷键支持**: 考虑添加快捷键快速清除会话
2. **自动清除**: 可考虑添加定时自动清除功能
3. **清除历史**: 记录清除操作的历史日志
4. **批量操作**: 支持清除所有相关数据（包括密码数据）
5. **通知机制**: 清除后通过系统通知提醒用户

---

_此功能增强了插件的安全性和用户体验，为用户提供了更灵活的会话管理能力。_

