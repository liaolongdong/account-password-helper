帮我优化账号密码快速填充助手（账号/用户名/用户号/手机/邮箱+密码）或者（手机号+短信验证码）输入框点击或者获取焦点时，显示快速填充侧边栏的逻辑
要求：

1. 性能良好且能覆盖绝大数（账号/用户名/用户号/手机/邮箱+加密）或者（手机号+短信验证码）登录场景，需要匹配弹窗登录场景；
2. 每次（账号/用户名/用户号/手机/邮箱+密码）或者（手机号+短信验证码）输入框点击或者获取焦点时，必须展示快速填充侧边栏；
3. 点击快速填充账号密码填充完成后自动隐藏快速填充侧边栏；
4. 当页面无法匹配到登录表单点击快速填充列表时，提示"当前页面未检测到登录表单"提示；
5. 动态切换登录表单以及弹窗登录表单有些场景未正常匹配到，可以通过增加检测表单页面或者弹窗是否存在登录按钮进行增强检测，防止遗漏场景；
6. 点击快速填充密码后，快速填充侧边栏未正常隐藏，这个问题能否修复？
7. 目前设置的主密码在会话有效期内是明文存储的，有安全隐患，需要改为加密存储，但是不能影响原有功能；
8. 快速填充侧边栏匹配域名的账号密码列表项排前面，没有设置url的账号密码列表排后面;
9. 点击快速填充用户名复制会穿透点击到密码自动填充导致快速填充侧边栏自动关闭，需要优化；
10. 多次点击悬浮按钮中的密码管理按钮会重复打开密码管理页面，帮我优化这个问题，如果已有打开的密码管理页面应该直接跳转到已打开的密码管理页面（待修复）

帮我优化本项目的代码，要求如下：

1. 去除无用代码和文件，优化重复代码，使用函数或者组件进行封装；
2. 优化后的代码不能影响现有功能逻辑、样式交互；
3. 必须使用各种依赖框架的最佳实践；

**代码质量优化：**

1. 清理无用代码和文件，消除重复代码，使用函数或组件进行合理封装
2. 优化后的代码不得影响现有的功能逻辑、样式和交互体验
3. 遵循所有依赖框架的最佳实践原则

技术栈包含Vue 3、TypeScript，项目结构包括components、entrypoints（options/popup/sidepanel）、utils等目录。请基于现有代码架构进行优化改进。
