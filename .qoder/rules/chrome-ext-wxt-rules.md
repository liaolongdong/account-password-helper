---
trigger: always_on
alwaysApply: true
---

1. 必须使用wxt + vue3 + typescript + element-plus + vite + tailwindcss等技术栈开发浏览器插件。
2. 必须符合这些技术栈的最佳编码实践。
3. 每次完成功能开发或者修复bug后，必须进行代码审核，确保代码质量，避免引入新的问题，保持代码的一致性和可维护性，必须执行自动化验证测试。
