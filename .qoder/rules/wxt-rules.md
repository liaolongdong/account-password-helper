---
trigger: always_on
---

1. 必须使用wxt + vue3 + typescript + element-plus + vite等技术栈开发浏览器插件，使用的技术栈必须是最新的稳定版本，且需按需引入，防止项目打包体积过大。
2. 生成的代码必须要有较强的可维护性、可扩展性、可复用性，，必须遵循良好的设计模式，必须符合这些技术栈的最佳编码实践，注释代码必须符合JSDoc注释文档规范。
3. 每次完成功能开发或者修复bug后，必须进行代码审核，确保代码质量，避免引入新的问题，保持代码的一致性和可维护性，必须执行自动化验证测试。
4. 必须使用ESLint + Prettier进行代码格式化和静态代码分析，确保代码风格一致性和可读性。
